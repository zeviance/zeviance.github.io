{
  "dependencies": {
    "html2canvas": "^1.4.1",
    "parcel": "^2.8.0",
    "stability-client": "^1.7.0"
  },
  "name": "umdemo",
  "version": "1.0.0",
  "scripts": {
    "start": "parcel ./src/index.html",
    "build": "parcel build ./src/index.html",
    "predeploy": "rm index.* && rm -rf dist && parcel build src/index.html --no-source-maps --public-url ./ && cp dist/index.* .",
    "deploy": "gh-pages -d dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^4.0.0",
    "shx": "^0.3.2"
  },
  "description": ""
}
